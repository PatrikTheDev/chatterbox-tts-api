# Chatterbox TTS Core Package (using VLLM version)
#chatterbox-tts
chatterbox-vllm @ git+https://github.com/randombk/chatterbox-vllm.git

# Required for Chatterbox TTS watermarker functionality
resemble-perth

# Required for model downloading (used by chatterbox-vllm)
huggingface_hub

# PyTorch with CPU support (ensure CPU compatibility)
torch>=2.0.0,<=2.7.1
torchaudio>=2.0.0,<=2.7.1

# FastAPI and ASGI server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Required for FastAPI file upload support
python-multipart>=0.0.6

# Environment variables
python-dotenv>=1.0.0

# Memory monitoring
psutil>=5.9.0

# Testing Dependencies
requests>=2.28.0 